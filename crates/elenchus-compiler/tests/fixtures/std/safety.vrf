// Safety domain. Also reuses the shared core invariants (so core/base.vrf is a
// diamond import: pulled by both engine.vrf and safety.vrf, merged only once).
DOMAIN car
IMPORT "../core/base.vrf"

PREMISE drive_safely:
    WHEN Car gear drive
    THEN Car brakes ok
