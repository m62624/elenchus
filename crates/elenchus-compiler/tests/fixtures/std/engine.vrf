// Engine domain. Reuses the shared core invariants via a relative import.
IMPORT "../core/base.vrf"

AXIOM engine_needs_fuel:
    WHEN Car engine running
    THEN Car has fuel

RULE running_implies_warm:
    WHEN Car engine running
    THEN Car engine warm
